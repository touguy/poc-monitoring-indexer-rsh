# 블록 이벤트 큐 처리 작업 TODO

- [x] 메모리 큐 변수 선언 (`blockQueue`)
- [x] 상태 변수 선언 (`isProcessingQueue`)
- [x] WebSocket 구독 로직 변경 (`wsService.onEvent('block')` 내 로직 수정)
- [x] 순차 비동기 워커 로직 구현 (`processBlockQueue()` 함수 생성)
- [x] 린트 에러 호환성 수정 (`onEvent` 콜백의 비동기 반환 타입 일치)
