- `[/]` 1. **Implement `onBlockHeader` in `BlockchainWsService`**
  - `[ ]` Add raw WS message interceptor for `newHeads`
- `[ ]` 2. **Refactor `BlockService` to use Header data**
  - `[ ]` Update `blockQueue` to store headers
  - `[ ]` Update `handleNewBlock` to use header if available and skip RPC
- `[ ]` 3. **Verify and Finalize**
  - `[ ]` Run `npm run build`
  - `[ ]` Update walkthrough documentation
