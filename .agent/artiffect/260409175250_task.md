# 작업 진행 현황 (Task)

- `[x]` `BlockRecordRepository`에 사용자 제안 SQL 기반의 `findMissingBlockGaps` 메서드 추가
- `[x]` `BlockService` > 웹소켓 훅 `handleNewBlock` 내부 복잡한 로직(재검증/갭 감지) 제거 및 직접 저장으로 단순화
- `[x]` `BlockService` > `setupPollingCron` 시간을 `*/60 * * * * *` 로 변경
- `[x]` `BlockService` > `handlePollingValidation` 내부에 SQL 기반 갭 탐지 루프(Step 3) 추가 및 복구 로직 구현
